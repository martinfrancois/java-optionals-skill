{
  "skill_name": "java-optionals",
  "evals": [
    {
      "id": 1,
      "prompt": "Use $java-optionals to improve this Java code without changing behavior:\n\n```java\nimport java.util.Optional;\n\nfinal class UserService {\n    String displayName(Optional<User> user) {\n        if (user.isPresent()) {\n            return user.get().displayName();\n        }\n        return \"Anonymous\";\n    }\n\n    record User(String displayName) {}\n}\n```",
      "expected_output": "Use Optional.map(...).orElse(...) to return the display name or Anonymous.",
      "files": [],
      "expectations": [
        "Replaces isPresent() plus get() with user.map(User::displayName).orElse(\"Anonymous\") or equivalent",
        "Does not introduce orElse(null) or a null check",
        "Does not convert the single Optional into a stream, list, loop, or collection",
        "Preserves the fallback value Anonymous"
      ]
    },
    {
      "id": 2,
      "prompt": "Use $java-optionals to make this upsert method easier to maintain without changing when update(...) or create(...) are called:\n\n```java\nimport java.util.Optional;\n\nfinal class WorkpadService {\n    Result upsert(Optional<Comment> existing, String text) {\n        if (existing.isPresent()) {\n            return update(existing.get(), text);\n        }\n        return create(text);\n    }\n\n    Result update(Comment comment, String text) { return new Result(\"updated\"); }\n    Result create(String text) { return new Result(\"created\"); }\n\n    record Comment(String id) {}\n    record Result(String status) {}\n}\n```",
      "expected_output": "Use existing.map(...).orElseGet(...) so create(text) remains lazy.",
      "files": [],
      "expectations": [
        "Uses map to handle the present Comment",
        "Uses orElseGet or another lazy branch for create(text)",
        "Does not use orElse(create(text))",
        "Does not introduce null-control-flow or a single-Optional collection workaround"
      ]
    },
    {
      "id": 3,
      "prompt": "Use $java-optionals to review this lookup. Create review.md with a short review decision and rationale. Do not modify the Java code.\n\n```java\nimport java.util.Optional;\nimport java.util.Set;\n\nfinal class CommandRedactor {\n    private static final Set<String> SECRET_OPTIONS = Set.of(\"--token\", \"--api-key\");\n\n    Optional<String> secretOption(String arg) {\n        return SECRET_OPTIONS.stream()\n                .filter(option -> arg.equals(option) || arg.startsWith(option + \"=\"))\n                .findAny();\n    }\n}\n```",
      "expected_output": "Keep the real collection stream and explain that it is not a single Optional workaround.",
      "files": [],
      "expectations": [
        "Recognizes SECRET_OPTIONS is a real collection source",
        "Keeps the stream lookup or explicitly says no refactor is needed",
        "Does not rewrite to a manual loop just to avoid an Optional terminal result",
        "Mentions findAny is appropriate because all matching options are equivalent"
      ]
    },
    {
      "id": 4,
      "prompt": "Use $java-optionals to review whether this Optional-returning lookup should change. Create review.md with a short review decision and rationale. Do not modify the Java code.\n\n```java\nimport java.util.List;\nimport java.util.Optional;\n\nfinal class RouteSelector {\n    Optional<Route> firstEnabledRoute(List<Route> routes) {\n        return routes.stream()\n                .filter(Route::enabled)\n                .findFirst();\n    }\n\n    record Route(String name, boolean enabled) {}\n}\n```",
      "expected_output": "Keep findFirst because firstEnabledRoute implies encounter-order priority.",
      "files": [],
      "expectations": [
        "Does not mechanically replace findFirst with findAny",
        "Identifies the method name and list order as an order-dependent contract",
        "Suggests testing two enabled routes if adding coverage",
        "Does not introduce a manual loop"
      ]
    },
    {
      "id": 5,
      "prompt": "Use $java-optionals to improve this lookup while preserving behavior:\n\n```java\nimport java.util.Optional;\nimport java.util.Set;\n\nfinal class FlagMatcher {\n    Optional<String> matchingFlag(Set<String> flags, String arg) {\n        return flags.stream()\n                .filter(flag -> arg.equals(flag) || arg.startsWith(flag + \"=\"))\n                .findFirst();\n    }\n}\n```",
      "expected_output": "Use findAny because the Set lookup has no first-match priority contract.",
      "files": [],
      "expectations": [
        "Changes findFirst to findAny or explains why findAny is the intended shape",
        "Recognizes the source is a real collection stream",
        "Does not introduce a manual loop or null-control-flow"
      ]
    },
    {
      "id": 6,
      "prompt": "Use $java-optionals to improve this method's Optional handling without changing its exception contract:\n\n```java\nimport java.io.IOException;\nimport java.util.Optional;\n\nfinal class WorkspaceSelector {\n    String workspaceId(Options options, Terminal terminal) throws IOException {\n        Optional<String> configured = options.workspaceId();\n        if (configured.isPresent()) {\n            return configured.get();\n        }\n        return promptForWorkspace(terminal);\n    }\n\n    String promptForWorkspace(Terminal terminal) throws IOException {\n        return terminal.readLine(\"Workspace: \");\n    }\n\n    interface Options { Optional<String> workspaceId(); }\n    interface Terminal { String readLine(String prompt) throws IOException; }\n}\n```",
      "expected_output": "Use a narrow explicit isEmpty branch and preserve throws IOException.",
      "files": [],
      "expectations": [
        "Preserves the checked IOException contract",
        "Does not wrap IOException in RuntimeException or UncheckedIOException",
        "Does not introduce a CheckedOptionals helper or Vavr dependency"
      ]
    },
    {
      "id": 7,
      "prompt": "Use $java-optionals to improve this lookup without changing its error behavior:\n\n```java\nimport java.util.Map;\nimport java.util.Optional;\n\nfinal class ConfigLookup {\n    String required(Map<String, String> values, String key) {\n        Optional<String> value = Optional.ofNullable(values.get(key));\n        if (value.isPresent()) {\n            return value.get();\n        }\n        throw new IllegalArgumentException(\"Missing config: \" + key);\n    }\n}\n```",
      "expected_output": "Use Optional.ofNullable(...).orElseThrow(...) and preserve the exception message.",
      "files": [],
      "expectations": [
        "Uses orElseThrow for true absence-as-error",
        "Preserves IllegalArgumentException",
        "Preserves the message Missing config: plus key",
        "Does not ban orElseThrow globally"
      ]
    },
    {
      "id": 8,
      "prompt": "Use $java-optionals to review this adapter. Create review.md with a short review decision and rationale. Do not modify the Java code.\n\n```java\nimport java.util.Optional;\n\nfinal class LegacyAdapter {\n    LegacyRequest toLegacy(Optional<String> comment) {\n        return new LegacyRequest(comment.orElse(null));\n    }\n\n    record LegacyRequest(String nullableComment) {}\n}\n```",
      "expected_output": "Allow isolated nullable interop if LegacyRequest genuinely uses null for absent, and do not add extra null-control-flow.",
      "files": [],
      "expectations": [
        "Distinguishes API-boundary nullable interop from local null-control-flow",
        "Does not blindly rewrite the boundary if the legacy API requires null",
        "Does not add a value != null branch around the nullable value",
        "Mentions that surrounding API changes would be a separate decision"
      ]
    },
    {
      "id": 9,
      "prompt": "Use $java-optionals to improve this method without changing behavior:\n\n```java\nimport java.util.Optional;\n\nfinal class CardMover {\n    Card moveIfNeeded(Optional<ListRef> target, Card card) {\n        if (target.isEmpty()) {\n            return card;\n        }\n        if (card.listId().equals(target.get().id())) {\n            return card;\n        }\n        return new Card(card.id(), target.get().id());\n    }\n\n    record Card(String id, String listId) {}\n    record ListRef(String id) {}\n}\n```",
      "expected_output": "Bind or map the selected value once; do not keep repeated target.get() calls.",
      "files": [],
      "expectations": [
        "Removes repeated target.get() calls after the guard",
        "Uses map plus a helper, or binds the value once after a narrow guard",
        "Preserves the no-op behavior when target is empty or already current",
        "Does not introduce null-control-flow"
      ]
    },
    {
      "id": 10,
      "prompt": "Use $java-optionals to improve this Optional-based fallback:\n\n```java\nimport java.util.Optional;\n\nfinal class GreetingService {\n    String greeting(Optional<String> name) {\n        for (String value : name.stream().toList()) {\n            return \"Hello \" + value;\n        }\n        return \"Hello guest\";\n    }\n}\n```",
      "expected_output": "Use name.map(...).orElse(...) and remove the loop over optional.stream().toList().",
      "files": [],
      "expectations": [
        "Recognizes the source is one Optional, not a real collection",
        "Uses map plus orElse or equivalent direct Optional boundary",
        "Removes stream().toList() and the loop",
        "Preserves both greeting strings"
      ]
    },
    {
      "id": 11,
      "prompt": "Use $java-optionals to write first-pass Java code for this requirement: return an existing cached document if present. If it is absent, create a document, store it in the cache, and return it.",
      "expected_output": "Use cache.find(key).orElseGet(() -> createAndStore(key)) or an equivalent lazy branch.",
      "files": [],
      "expectations": [
        "Chooses the Optional boundary before writing presence-check branches",
        "Keeps document creation and cache mutation lazy with orElseGet or an explicit absent branch",
        "Does not use orElse(createAndStore(key))",
        "Does not use isPresent() plus get() or an Optional-to-list workaround"
      ]
    },
    {
      "id": 12,
      "prompt": "Use $java-optionals to improve this selection code without changing behavior:\n\n```java\nimport java.nio.file.Path;\nimport java.util.List;\nimport java.util.Optional;\n\nfinal class DiagnosticSelector {\n    Selection select(Manifest manifest, Optional<String> board, Optional<Path> workflow, Path configDir) {\n        if (board.isPresent() && workflow.isPresent()) {\n            throw new IllegalArgumentException(\"--board and --workflow cannot be used together.\");\n        }\n        if (board.isPresent()) {\n            return byBoard(manifest, board.orElseThrow());\n        }\n        if (workflow.isPresent()) {\n            return byWorkflow(manifest, workflow.orElseThrow(), configDir);\n        }\n        return new Selection(\"none\", manifest.boards(), Optional.empty());\n    }\n\n    Selection byBoard(Manifest manifest, String board) { return new Selection(\"board\", List.of(), Optional.empty()); }\n    Selection byWorkflow(Manifest manifest, Path workflow, Path configDir) { return new Selection(\"workflow\", List.of(), Optional.of(workflow)); }\n    record Manifest(List<String> boards) {}\n    record Selection(String kind, List<String> boards, Optional<Path> workflow) {}\n}\n```",
      "expected_output": "Keep the boolean-only conflict check, then use board.map(...).orElseGet(...) with a nested workflow Optional boundary.",
      "files": [],
      "expectations": [
        "Keeps the board/workflow mutual-exclusion check and preserves its exception message",
        "Recognizes that boolean-only presence validation may stay as isPresent() checks",
        "Removes presence-check plus orElseThrow value reads from selector branches",
        "Does not introduce orElse(null) or a local null branch",
        "Does not convert either single Optional selector to stream().toList(), a list, or a loop"
      ]
    },
    {
      "id": 13,
      "prompt": "Use $java-optionals to improve this output handling. Create ReportCommand.java containing the revised class:\n\n```java\nimport java.nio.file.Path;\nimport java.util.Optional;\n\nfinal class ReportCommand {\n    void finish(Optional<Path> output, String report) {\n        if (output.isPresent()) {\n            write(output.orElseThrow(), report);\n        } else {\n            print(report);\n        }\n    }\n\n    void write(Path path, String report) {}\n    void print(String report) {}\n}\n```",
      "expected_output": "Use output.ifPresentOrElse(path -> write(path, report), () -> print(report)) or an equivalent side-effect boundary.",
      "files": [],
      "expectations": [
        "Uses ifPresentOrElse or an equivalent Optional side-effect boundary",
        "Removes isPresent() plus orElseThrow value read",
        "Preserves both write and print branches",
        "Does not introduce null-control-flow or a fake collection workaround"
      ]
    },
    {
      "id": 14,
      "prompt": "Use $java-optionals to review this proposed cleanup. Create review.md with a short review decision and rationale.\n\nBefore:\n\n```java\nimport java.util.Optional;\n\nfinal class UserService {\n    String displayName(Optional<User> user) {\n        if (user.isPresent()) {\n            return user.get().displayName();\n        }\n        return \"Anonymous\";\n    }\n\n    record User(String displayName) {}\n}\n```\n\nProposed:\n\n```java\nimport java.util.Optional;\n\nfinal class UserService {\n    String displayName(Optional<User> user) {\n        User value = user.orElse(null);\n        if (value != null) {\n            return value.displayName();\n        }\n        return \"Anonymous\";\n    }\n\n    record User(String displayName) {}\n}\n```",
      "expected_output": "Reject the proposed cleanup because it replaces isPresent()+get() with orElse(null)+null branching; suggest user.map(User::displayName).orElse(\"Anonymous\").",
      "files": [],
      "expectations": [
        "Clearly says the proposed change should not be accepted",
        "Explains that orElse(null) plus value != null is nullable control flow in another form",
        "Suggests user.map(User::displayName).orElse(\"Anonymous\") or an equivalent Optional boundary",
        "Preserves the Anonymous fallback"
      ]
    },
    {
      "id": 15,
      "prompt": "Use $java-optionals to review this proposed rewrite. Create review.md with a short review decision and rationale.\n\nBefore:\n\n```java\nimport java.util.Optional;\nimport java.util.Set;\n\nfinal class CommandOptionMatcher {\n    private static final Set<String> REDACTED_VALUE_OPTIONS = Set.of(\n            \"--token\", \"--key\", \"--workflow\", \"--config-dir\", \"--state-home\", \"--output\");\n\n    static Optional<String> redactedCommandValueOption(String arg) {\n        return REDACTED_VALUE_OPTIONS.stream()\n                .filter(option -> arg.equals(option) || arg.startsWith(option + \"=\"))\n                .findAny();\n    }\n}\n```\n\nProposed:\n\n```java\nimport java.util.Optional;\nimport java.util.Set;\n\nfinal class CommandOptionMatcher {\n    private static final Set<String> REDACTED_VALUE_OPTIONS = Set.of(\n            \"--token\", \"--key\", \"--workflow\", \"--config-dir\", \"--state-home\", \"--output\");\n\n    static Optional<String> redactedCommandValueOption(String arg) {\n        for (String option : REDACTED_VALUE_OPTIONS) {\n            if (arg.equals(option) || arg.startsWith(option + \"=\")) {\n                return Optional.of(option);\n            }\n        }\n        return Optional.empty();\n    }\n}\n```",
      "expected_output": "Reject the proposed loop rewrite; keep the stream over the real Set and findAny because any matching redacted option is equivalent.",
      "files": [],
      "expectations": [
        "Says the proposed loop rewrite is unnecessary or should not be accepted",
        "Recognizes REDACTED_VALUE_OPTIONS is a real collection source",
        "Keeps or recommends the stream with findAny",
        "Does not claim Optional-returning streams should generally become loops"
      ]
    },
    {
      "id": 16,
      "prompt": "Use $java-optionals to review this proposed helper-based refactor. Create review.md with a short review decision and rationale.\n\nBefore:\n\n```java\nimport java.io.IOException;\nimport java.util.Optional;\n\nfinal class WorkspaceSelector {\n    String workspaceId(Options options, Terminal terminal) throws IOException {\n        Optional<String> configured = options.workspaceId();\n        if (configured.isPresent()) {\n            return configured.get();\n        }\n        return promptForWorkspace(terminal);\n    }\n\n    String promptForWorkspace(Terminal terminal) throws IOException {\n        return terminal.readLine(\"Workspace: \");\n    }\n\n    interface Options { Optional<String> workspaceId(); }\n    interface Terminal { String readLine(String prompt) throws IOException; }\n}\n```\n\nProposed:\n\n```java\nimport java.io.IOException;\nimport java.io.UncheckedIOException;\nimport java.util.Optional;\nimport java.util.function.Function;\n\nfinal class WorkspaceSelector {\n    String workspaceId(Options options, Terminal terminal) throws IOException {\n        return CheckedOptionals.mapOrElseGet(\n                options.workspaceId(),\n                id -> id,\n                () -> promptForWorkspace(terminal));\n    }\n\n    String promptForWorkspace(Terminal terminal) throws IOException {\n        return terminal.readLine(\"Workspace: \");\n    }\n\n    interface Options { Optional<String> workspaceId(); }\n    interface Terminal { String readLine(String prompt) throws IOException; }\n}\n\nfinal class CheckedOptionals {\n    static <T, R> R mapOrElseGet(Optional<T> value, Function<T, R> present, CheckedSupplier<R> absent)\n            throws IOException {\n        try {\n            return value.map(present).orElseGet(() -> unchecked(absent));\n        } catch (UncheckedIOException e) {\n            throw e.getCause();\n        }\n    }\n\n    private static <R> R unchecked(CheckedSupplier<R> supplier) {\n        try {\n            return supplier.get();\n        } catch (IOException e) {\n            throw new UncheckedIOException(e);\n        }\n    }\n\n    @FunctionalInterface\n    interface CheckedSupplier<T> {\n        T get() throws IOException;\n    }\n}\n```",
      "expected_output": "Reject the helper-based local cleanup; keep a narrow explicit checked-IO branch and do not add unchecked wrappers or Vavr-style dependencies for this case.",
      "files": [],
      "expectations": [
        "Says the helper-based refactor should not be accepted as a narrow local cleanup",
        "Explains that prompting is checked IO and should remain visible",
        "Rejects UncheckedIOException adaptation solely to keep fluent Optional syntax",
        "Suggests an explicit isEmpty branch with configured.orElseThrow() or equivalent",
        "Does not suggest adding Vavr or another functional dependency for this local cleanup"
      ]
    },
    {
      "id": 17,
      "prompt": "Use $java-optionals to create AssigneeFormatter.java.\n\nImplement a small Java class AssigneeFormatter with:\n\n```java\nString label(Optional<User> assignee)\n```\n\nRules:\n\n- If an assignee is present and handle() is not blank, return \"@\" + handle.\n- If an assignee is present but handle() is blank, return the user's displayName().\n- If no assignee is present, return \"unassigned\".\n\nInclude this nested record:\n\n```java\nrecord User(String handle, String displayName) {}\n```",
      "expected_output": "Create AssigneeFormatter.java using assignee.map(...).orElse(\"unassigned\") or an equivalent direct Optional boundary, with no isPresent()+get(), null, or fake collection workaround.",
      "files": [],
      "expectations": [
        "Creates AssigneeFormatter.java with AssigneeFormatter, label(Optional<User>), and nested User record",
        "Uses assignee.map(...).orElse(\"unassigned\") or an equivalent direct Optional boundary",
        "Does not use isPresent()/isEmpty() plus get() or orElseThrow() for ordinary control flow",
        "Does not use orElse(null), local null branching, Optional.stream().toList(), or a loop over one Optional",
        "Returns @handle for present nonblank handles, displayName for present blank handles, and unassigned when absent"
      ]
    },
    {
      "id": 18,
      "prompt": "Use $java-optionals to create DiagnosticSelector.java.\n\nImplement:\n\n```java\nSelection select(Optional<String> board, Optional<Path> workflow, List<String> allBoards)\n```\n\nRules:\n\n- If both selectors are present, throw IllegalArgumentException(\"--board and --workflow cannot be used together.\").\n- If board is present, return new Selection(\"board\", board, Optional.empty()).\n- If workflow is present, return new Selection(\"workflow\", Optional.empty(), workflow).\n- If neither selector is present, return new Selection(\"all\", Optional.empty(), Optional.empty()).\n\nInclude imports and this nested record:\n\n```java\nrecord Selection(String kind, Optional<String> board, Optional<Path> workflow) {}\n```",
      "expected_output": "Create DiagnosticSelector.java with a boolean-only conflict check and direct Optional boundaries for board/workflow value branches.",
      "files": [],
      "expectations": [
        "Creates DiagnosticSelector.java with select(Optional<String>, Optional<Path>, List<String>) and Selection record",
        "Preserves the exact conflict exception message",
        "Uses map/orElseGet or another direct Optional/value-binding shape for board and workflow branches after the conflict check",
        "Does not use isPresent()/isEmpty() followed by get() or orElseThrow() for selector value branches",
        "Does not use null or fake collection/list/loop workarounds"
      ]
    },
    {
      "id": 19,
      "prompt": "Use $java-optionals to create CommandOptionMatcher.java.\n\nImplement:\n\n```java\nstatic Optional<String> redactedValueOption(String arg)\n```\n\nThe class should contain this option set:\n\n```java\nprivate static final Set<String> REDACTED_VALUE_OPTIONS = Set.of(\n        \"--token\", \"--key\", \"--workflow\", \"--config-dir\", \"--state-home\", \"--output\");\n```\n\nReturn the matching option when arg equals the option or starts with option + \"=\". Return Optional.empty() otherwise.",
      "expected_output": "Use a readable stream over REDACTED_VALUE_OPTIONS with findAny and no null sentinel.",
      "files": [],
      "expectations": [
        "Creates CommandOptionMatcher.java with the requested set and method",
        "Uses a real collection stream or equivalent readable real collection lookup",
        "Uses findAny because any matching redacted option is equivalent",
        "Matches exact option arguments and option=value forms",
        "Does not use null sentinels or local null-control-flow"
      ]
    },
    {
      "id": 20,
      "prompt": "Use $java-optionals to review this adapter. Create review.md with a short review decision and rationale. Do not modify the Java code.\n\n```java\nimport java.util.Optional;\n\nfinal class AuditAdapter {\n    AuditEvent toLegacy(Optional<String> comment) {\n        return new AuditEvent(comment.orElse(null));\n    }\n\n    record AuditEvent(String nullableComment) {}\n}\n```",
      "expected_output": "Allow the isolated nullable interop boundary if AuditEvent uses null for absence; do not add null branching or bundle an API redesign.",
      "files": [],
      "expectations": [
        "Recognizes this can be acceptable if AuditEvent uses null to represent absent comments",
        "Does not suggest extra value != null branching",
        "Separates any AuditEvent redesign as a separate API/design decision",
        "Does not claim every orElse(null) is forbidden regardless of boundary context"
      ]
    },
    {
      "id": 21,
      "prompt": "Use $java-optionals to review this proposed cleanup. Create review.md with a short review decision and rationale.\n\nBefore:\n\n```java\nimport java.util.Optional;\n\nfinal class LegacyAdapter {\n    LegacyRequest toLegacy(Optional<String> comment) {\n        return new LegacyRequest(comment.orElse(null));\n    }\n\n    record LegacyRequest(String nullableComment) {}\n}\n```\n\nProposed:\n\n```java\nimport java.util.Optional;\n\nfinal class LegacyAdapter {\n    LegacyRequest toLegacy(Optional<String> comment) {\n        return new LegacyRequest(comment);\n    }\n\n    record LegacyRequest(Optional<String> comment) {}\n}\n```",
      "expected_output": "Reject bundling the LegacyRequest API/DTO change into a local Optional cleanup.",
      "files": [],
      "expectations": [
        "Says the proposed change should not be accepted as a local Optional cleanup",
        "Recognizes the original orElse(null) can be isolated legacy interop",
        "Does not endorse changing the DTO/record field to Optional as part of this cleanup",
        "Frames DTO redesign as separate scoped API/design work"
      ]
    },
    {
      "id": 22,
      "prompt": "Use $java-optionals to review this proposed change. Create review.md with a short review decision and rationale.\n\nBefore:\n\n```java\nimport java.util.List;\nimport java.util.Optional;\n\nfinal class ContactSelector {\n    Optional<Contact> primaryContact(List<Contact> contacts) {\n        return contacts.stream()\n                .filter(Contact::verified)\n                .findFirst();\n    }\n\n    record Contact(String email, boolean verified) {}\n}\n```\n\nProposed:\n\n```java\nimport java.util.List;\nimport java.util.Optional;\n\nfinal class ContactSelector {\n    Optional<Contact> primaryContact(List<Contact> contacts) {\n        return contacts.stream()\n                .filter(Contact::verified)\n                .findAny();\n    }\n\n    record Contact(String email, boolean verified) {}\n}\n```",
      "expected_output": "Reject the findAny change because primaryContact and List encounter order imply priority.",
      "files": [],
      "expectations": [
        "Says the proposed findAny change should not be accepted",
        "Explains that primaryContact plus List order implies first verified contact priority",
        "Does not rewrite the stream to a loop",
        "Mentions testing two verified contacts where the earlier one wins or otherwise notes priority behavior"
      ]
    },
    {
      "id": 23,
      "prompt": "Use $java-optionals to create DeliveryRouter.java.\n\nImplement:\n\n```java\nvoid route(Optional<URI> webhook, Message message)\n```\n\nRules:\n\n- If webhook is present, call sendWebhook(webhookUri, message).\n- If webhook is absent, call enqueueLocal(message).\n\nInclude these members:\n\n```java\nvoid sendWebhook(URI webhookUri, Message message) {}\nvoid enqueueLocal(Message message) {}\nrecord Message(String body) {}\n```",
      "expected_output": "Use webhook.ifPresentOrElse(...) or an equivalent Optional side-effect boundary with no value-read branch.",
      "files": [],
      "expectations": [
        "Creates DeliveryRouter.java with route(Optional<URI>, Message), sendWebhook, enqueueLocal, and Message record",
        "Uses ifPresentOrElse or an equivalent Optional side-effect boundary",
        "Does not use isPresent()/isEmpty() followed by get() or orElseThrow() for the webhook URI",
        "Does not use null or fake collection/list/loop workarounds",
        "Calls sendWebhook only when webhook is present and enqueueLocal only when absent"
      ]
    }
  ]
}
